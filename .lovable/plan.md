

# Integrate Google Tag Manager (GTM-5S9B9TRP)

## Change

Add the two GTM snippets to `index.html`:
1. The `<script>` tag high in `<head>`
2. The `<noscript>` iframe immediately after the opening `<body>` tag

**File: `index.html`** — only file changed. Two insertions using the exact snippets from Google's instructions.

